{% extends 'base.html.twig' %}

{% form_theme form _self %}




{% block _recommendation_description_widget %}
    <markdown-editor id="{{ id }}" name="{{ full_name }}" value="{{ value }}">

    </markdown-editor>
{% endblock %}

{% block _recommendation_implementation_widget %}
    <markdown-editor id="{{ id }}" name="{{ full_name }}" value="{{ value }}">

    </markdown-editor>
{% endblock %}

{% block _recommendation_tips_widget %}
    <markdown-editor id="{{ id }}" name="{{ full_name }}" value="{{ value }}">

    </markdown-editor>
{% endblock %}

{% block _recommendation_tipsSchoolManagement_widget %}
    <markdown-editor id="{{ id }}" name="{{ full_name }}" value="{{ value }}">

    </markdown-editor>
{% endblock %}

{% block _recommendation_examples_widget %}
    <markdown-editor id="{{ id }}" name="{{ full_name }}" value="{{ value }}">

    </markdown-editor>
{% endblock %}

{% block _recommendation_additionalInformation_widget %}
    <markdown-editor id="{{ id }}" name="{{ full_name }}" value="{{ value }}">

    </markdown-editor>
{% endblock %}

{% block body %}
    <h1>Handlungsempfehlung bearbeiten</h1>
    <div class="row">
        <div class="col">
            <div class="panel">
                <h3>{{ 'Handlungsempfehlung'|trans }}</h3>

                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_row(form.advanced) }}
                {{ form_widget(form) }}
                <div class="form-group row">
                    <div class="col-sm-2"></div>
                    <div class="col-sm-10 d-flex">
                        <button type="submit" id="save" name="save" class="btn-primary btn">Speichern</button>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
