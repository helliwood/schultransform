{% extends 'base.html.twig' %}

{% block body %}
<div class="row mb-3">
    <div class="col">
        <div class="panel">
            <h2>SpamMails</h2>

<!--<div class="my-2 text-right">
            <button class="btn mr-2 btn-danger btn-sm my-grid mr-grid"><i class="fas fa-trash"></i> alle <strong>weitergeleiteten</strong> mails löschen</button>
            <button class="btn mr-2 btn-danger btn-sm my-grid"><i class="fas fa-trash"></i> alle <strong>gelesenen</strong> mails löschen</button>
</div>-->
            <spam-table
                    api-url="{{ path('TFSecurity_get_spam') }}"
                    user-mail="  {{ app.user.Email }}"

            >
            </spam-table>
        </div>
    </div>
</div>
{% endblock %}
