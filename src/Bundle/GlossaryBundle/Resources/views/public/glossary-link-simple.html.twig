{% if data.href|default(null) and data.wordDisplayed|default(null) %}<a title="{% if data.isRelatedWord|default(null) %}Dieses Begriff ist verbunden mit dem Wort {{ data.word }}.{% else %}{{ data.wordDisplayed }}{% endif %}" alt="{{ data.wordDisplayed }}" class="{% if data.isRelatedWord|default(null) %}text-secondary{% else %}text-primary{% endif %}" href="{{ data.href }}"><b>{{ data.wordDisplayed }}</b></a>{% endif %}